{% extends 'MeraPostBundle:PostOffice:post.html.twig' %}
{% block post %}
    <h1>Редактирование почтового отделения</h1>
    <div class="btn-toolbar">
        <div class="btn-group">
            <a class="btn" href="{{ path('postoffice') }}"><i class="icon-th-list"></i> К
                списку</a>
            <a class="btn" href="{{ path('postoffice_delete', { 'id': entity.id }) }}"><i class="icon-remove""></i>
                Удалить</a>
        </div>
    </div>

    <form action="{{ path('postoffice_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p>
            <button class="btn btn-primary" type="submit"><i class="icon-ok icon-white"></i> Сохранить</button>
        </p>
    </form>

{% endblock %}