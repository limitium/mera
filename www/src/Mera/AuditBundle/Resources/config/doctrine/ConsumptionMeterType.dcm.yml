ConsumptionMeterType:
  type: entity
  fields:
    id:
      id: true
      type: integer
      generator:
        strategy: AUTO
    name:
      type: string
      nullable: false
  oneToMany:
    ConsumptionMeters:
      targetEntity: ConsumptionMeter
      mappedBy: ConsumptionMeterType
